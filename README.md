# Hidden Wallet

**Слой безопасности для вашего Solana кошелька**

Hidden Wallet — это дополнительный слой безопасности, который скрывает ваш основной Solana кошелек, обеспечивая приватность и защиту транзакций.

## 🎯 Основные возможности

- **Приватность транзакций**: Скрытие реального адреса кошелька
- **Шифрование данных**: Защита приватных ключей и данных
- **Обфускация трафика**: Маскировка сетевой активности
- **Многоуровневая защита**: Несколько слоев безопасности
- **Децентрализованная архитектура**: Без единой точки отказа

## 📚 Документация

Полная документация доступна в папке [docs](./docs/):

- **[Обзор](./docs/OVERVIEW.md)** - Введение в Hidden Wallet и основные концепции
- **[Возможности](./docs/FEATURES.md)** - Полный список функций и возможностей
- **[Быстрый старт](./docs/GETTING_STARTED.md)** - Руководство по установке и первому использованию
- **[Архитектура](./docs/ARCHITECTURE.md)** - Детальное описание архитектуры системы
- **[API Reference](./docs/API.md)** - Полная документация API
- **[Интеграция](./docs/INTEGRATION.md)** - Руководство по интеграции с вашим приложением
- **[Примеры](./docs/EXAMPLES.md)** - Примеры использования и кода
- **[Безопасность](./docs/SECURITY.md)** - Руководство по безопасности

### Быстрый старт

1. **Установка зависимостей**
```bash
npm install
```

2. **Настройка окружения**
```bash
cp .env.example .env
# Заполните необходимые переменные окружения
```

3. **Запуск приложения**
```bash
npm run dev
```

### Архитектура

Hidden Wallet состоит из следующих компонентов:

#### 1. Wallet Layer (Слой кошелька)
- Управление подключением к Solana кошельку
- Обработка транзакций
- Управление ключами

#### 2. Security Layer (Слой безопасности)
- Шифрование приватных ключей
- Обфускация адресов
- Защита от анализа трафика

#### 3. Privacy Layer (Слой приватности)
- Смешивание транзакций
- Скрытие связей между адресами
- Защита метаданных

#### 4. Proxy Layer (Прокси слой)
- Маршрутизация транзакций
- Распределение нагрузки
- Анонимизация запросов

### Использование

#### Подключение кошелька

```typescript
import { HiddenWallet } from '@hidden-wallet/core';

const wallet = new HiddenWallet({
  provider: 'phantom', // или 'solflare', 'ledger'
  network: 'mainnet-beta',
  securityLevel: 'high'
});

await wallet.connect();
```

#### Создание скрытого адреса

```typescript
const hiddenAddress = await wallet.createHiddenAddress();
console.log('Hidden Address:', hiddenAddress);
```

#### Отправка транзакции

```typescript
const transaction = await wallet.sendTransaction({
  to: 'destination_address',
  amount: 1.5, // SOL
  hideOrigin: true // Скрыть источник транзакции
});
```

#### Получение баланса

```typescript
const balance = await wallet.getBalance();
console.log('Balance:', balance);
```

### Безопасность

#### Уровни защиты

1. **Базовый (Basic)**
   - Шифрование ключей
   - Базовая обфускация

2. **Стандартный (Standard)**
   - Расширенное шифрование
   - Обфускация трафика
   - Смешивание транзакций

3. **Высокий (High)**
   - Полное шифрование
   - Многоуровневая обфускация
   - Децентрализованная маршрутизация
   - Защита от анализа

#### Рекомендации по безопасности

- ✅ Всегда используйте сильные пароли
- ✅ Храните seed-фразу в безопасном месте
- ✅ Регулярно обновляйте приложение
- ✅ Используйте аппаратные кошельки для больших сумм
- ✅ Включайте двухфакторную аутентификацию
- ❌ Не делитесь приватными ключами
- ❌ Не используйте один пароль для всех сервисов
- ❌ Не храните ключи в облаке без шифрования

### API Reference

#### HiddenWallet Class

##### Constructor

```typescript
new HiddenWallet(options: WalletOptions)
```

**Параметры:**
- `provider`: Тип провайдера ('phantom' | 'solflare' | 'ledger' | 'custom')
- `network`: Сеть Solana ('mainnet-beta' | 'devnet' | 'testnet')
- `securityLevel`: Уровень безопасности ('basic' | 'standard' | 'high')

##### Методы

**connect()**
Подключает кошелек к приложению.

```typescript
await wallet.connect();
```

**disconnect()**
Отключает кошелек.

```typescript
await wallet.disconnect();
```

**createHiddenAddress()**
Создает новый скрытый адрес.

```typescript
const address = await wallet.createHiddenAddress();
```

**sendTransaction(options)**
Отправляет транзакцию через скрытый слой.

```typescript
const tx = await wallet.sendTransaction({
  to: string,
  amount: number,
  hideOrigin?: boolean,
  priorityFee?: number
});
```

**getBalance()**
Получает баланс кошелька.

```typescript
const balance = await wallet.getBalance();
```

**getTransactionHistory()**
Получает историю транзакций.

```typescript
const history = await wallet.getTransactionHistory({
  limit?: number,
  before?: string
});
```

### Конфигурация

#### Переменные окружения

```env
# Solana Network
NEXT_PUBLIC_SOLANA_NETWORK=mainnet-beta
NEXT_PUBLIC_SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# Security
NEXT_PUBLIC_SECURITY_LEVEL=high
NEXT_PUBLIC_ENABLE_ENCRYPTION=true

# Privacy
NEXT_PUBLIC_ENABLE_MIXING=true
NEXT_PUBLIC_MIXING_POOL_SIZE=100

# Proxy
NEXT_PUBLIC_PROXY_ENABLED=true
NEXT_PUBLIC_PROXY_NODES=https://proxy1.example.com,https://proxy2.example.com
```

### Разработка

#### Структура проекта

```
hidden-wallet/
├── src/
│   ├── core/           # Основная логика
│   ├── security/       # Слой безопасности
│   ├── privacy/        # Слой приватности
│   ├── proxy/          # Прокси слой
│   ├── wallet/         # Интеграция с кошельками
│   ├── components/     # UI компоненты
│   └── hooks/          # React hooks
├── docs/               # Документация
├── tests/              # Тесты
└── public/             # Статические файлы
```

#### Запуск тестов

```bash
npm test
```

#### Сборка

```bash
npm run build
```

### FAQ

**Q: Безопасно ли использовать Hidden Wallet?**
A: Hidden Wallet использует проверенные криптографические методы и не хранит ваши приватные ключи. Все операции выполняются локально.

**Q: Могу ли я использовать свой существующий кошелек?**
A: Да, Hidden Wallet работает как слой поверх вашего существующего Solana кошелька.

**Q: Влияет ли это на скорость транзакций?**
A: Дополнительные слои безопасности могут немного увеличить время обработки транзакций, но обычно это незначительно.

**Q: Совместим ли Hidden Wallet с другими приложениями?**
A: Hidden Wallet совместим со стандартными Solana кошельками и приложениями.

### Лицензия

MIT License

### Поддержка

Для вопросов и поддержки:
- GitHub Issues: [github.com/hidden-wallet/issues](https://github.com/hidden-wallet/issues)
- Email: support@hidden-wallet.com
- Discord: [discord.gg/hidden-wallet](https://discord.gg/hidden-wallet)

